# OAuth Configuration
# Copy this file to .env.local and fill in your actual values
# Environment variables prefixed with VITE_ are exposed to the frontend

# Google OAuth (Gmail)
VITE_GOOGLE_CLIENT_ID=your_google_client_id_here

# Microsoft OAuth (Outlook)
VITE_MICROSOFT_CLIENT_ID=your_microsoft_client_id_here

# Note: Client secrets should NEVER be exposed to the frontend
# They are only used in the backend (Azure Functions)
# Backend environment variables are configured in:
# - api/local.settings.json (for local development)
# - Azure Static Web App Configuration (for production)
