name: Azure Static Web Apps CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - main

jobs:
  build_and_deploy_job:
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    name: Build and Deploy Job
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
      
      - name: Set up Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
      
      - name: Verify Node.js version
        run: |
          node --version
          npm --version
          echo "Node.js version verified"
      
      - name: Install frontend dependencies
        run: npm ci
      
      - name: Build frontend application
        run: npm run build
      
      - name: Set up Node.js 20 for API build
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: api/package-lock.json
      
      - name: Install API dependencies
        working-directory: ./api
        run: npm ci --production
      
      - name: Build API
        working-directory: ./api
        run: npm run build
      
      - name: Clean up before deployment
        run: |
          echo "Cleaning up unnecessary files to reduce deployment size and file count..."
          # Remove frontend node_modules (not needed in dist)
          rm -rf node_modules
          
          # Remove API source files, keep only built JS files
          find api -name "*.ts" -type f -delete 2>/dev/null || true
          find api -name "tsconfig.json" -type f -delete 2>/dev/null || true
          find api -name ".nvmrc" -type f -delete 2>/dev/null || true
          
          # Aggressively clean API node_modules - remove unnecessary files
          if [ -d "api/node_modules" ]; then
            echo "Cleaning API node_modules..."
            # Remove test files, docs, and source maps
            find api/node_modules -name "*.test.js" -delete 2>/dev/null || true
            find api/node_modules -name "*.spec.js" -delete 2>/dev/null || true
            find api/node_modules -name "*.map" -delete 2>/dev/null || true
            find api/node_modules -name "README*" -delete 2>/dev/null || true
            find api/node_modules -name "CHANGELOG*" -delete 2>/dev/null || true
            find api/node_modules -name "LICENSE*" -delete 2>/dev/null || true
            find api/node_modules -name "*.md" -delete 2>/dev/null || true
            find api/node_modules -type d -name "test" -exec rm -rf {} + 2>/dev/null || true
            find api/node_modules -type d -name "tests" -exec rm -rf {} + 2>/dev/null || true
            find api/node_modules -type d -name "__tests__" -exec rm -rf {} + 2>/dev/null || true
            find api/node_modules -type d -name "docs" -exec rm -rf {} + 2>/dev/null || true
            find api/node_modules -type d -name "doc" -exec rm -rf {} + 2>/dev/null || true
            find api/node_modules -type d -name "examples" -exec rm -rf {} + 2>/dev/null || true
            find api/node_modules -type d -name "example" -exec rm -rf {} + 2>/dev/null || true
            echo "API node_modules cleaned"
          fi
          
          # Remove Oryx build artifacts
          rm -rf api/.oryx* 2>/dev/null || true
          
          # Remove unnecessary root files and folders
          find . -maxdepth 1 -type f \( -name "*.md" -o -name "*.log" -o -name ".swaignore" -o -name ".gitignore" \) -delete 2>/dev/null || true
          rm -rf .github 2>/dev/null || true
          rm -rf src 2>/dev/null || true
          rm -rf public 2>/dev/null || true
          
          # Count files and calculate size
          echo "---"
          echo "File count:"
          find . -type f | wc -l
          echo "Deployment size:"
          du -sh . || echo "Size calculation unavailable"
          echo "---"
          echo "Cleanup complete"
      
      - name: Deploy to Azure Static Web Apps
        id: builddeploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: "upload"
          app_location: "/" # App source code path
          api_location: "api" # Api source code path
          output_location: "dist" # Built app content directory
          skip_app_build: true # We already built the frontend above
          skip_api_build: true # We already built the API above

  close_pull_request_job:
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    name: Close Pull Request Job
    steps:
      - name: Close Pull Request
        id: closepullrequest
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          action: "close"

