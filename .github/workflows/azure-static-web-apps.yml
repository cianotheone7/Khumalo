name: Azure Static Web Apps CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - main

jobs:
  build_and_deploy_job:
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    name: Build and Deploy Job
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
      
      - name: Set up Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
      
      - name: Install frontend dependencies
        run: npm ci
      
      - name: Build frontend application
        env:
          VITE_API_BASE_URL: ${{ secrets.VITE_API_BASE_URL || 'https://medical-practice-api-v2.azurewebsites.net' }}
        run: npm run build
      
      - name: Clean up workspace - move dist to root
        run: |
          echo "Cleaning up workspace - moving dist contents to root"
          # Save config first
          mv staticwebapp.config.json .temp_config.json 2>/dev/null || true
          # FORCE REMOVE API FOLDER FIRST
          rm -rf api
          # Move everything from dist to root
          mv dist/* . 2>/dev/null || true
          mv dist/.* . 2>/dev/null || true
          # Remove everything except dist contents and config
          rm -rf dist node_modules src public .github
          rm -f *.md package*.json *.lock *.ts *.js vite.config.* tsconfig.* eslint.* 2>/dev/null || true
          # Restore config to root
          mv .temp_config.json staticwebapp.config.json 2>/dev/null || true
          # VERIFY API IS GONE
          if [ -d "api" ]; then
            echo "ERROR: API folder still exists!"
            exit 1
          fi
          echo "✓ API folder removed"
          echo "Workspace cleaned - only frontend files remain"
          echo "File count:"
          find . -type f | wc -l
          echo "Contents:"
          ls -la
      
      - name: Deploy to Azure Static Web Apps (Frontend Only)
        id: builddeploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: "upload"
          app_location: "/"
          output_location: "/"
          skip_app_build: true

  close_pull_request_job:
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    name: Close Pull Request Job
    steps:
      - name: Close Pull Request
        id: closepullrequest
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          action: "close"

